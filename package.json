{
  "name": "nexusai",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "compile": "tsc  --build --force tsconfig.json"
  }
}
